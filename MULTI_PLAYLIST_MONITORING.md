# 单账号多歌单监控

每套实例只连接一个网易账号，用户从该账号可访问的歌单中勾选 1–20 个非空歌单。首次窗口预选第一个非空项；再次管理时保留现有勾选。取消监控项会先列出名称并二次确认。新增歌单和切换账号都在临时基线完整建立后才通过一次 D1 事务启用；准备失败不会修改正式选择。“从网易云重建基线”可为当前账号全部已选歌单重新获取完整快照，成功后才一次性替换旧歌曲状态、待找回和旧同步记录；失败时旧数据不变。

## 数据与状态

`songs` 按网易歌曲 ID 保存共享资料。`monitored_playlists` 保存歌单资料、网易列表顺序及独立的 `baseline_established` 标志。`playlist_song_states` 用 `(playlist_id, song_id)` 联合主键保存每首歌在每个歌单中的正常、变灰或消失状态；不会按照勾选数量动态建表。取消监控会级联删除该歌单状态，未被其他歌单引用的歌曲资料才会清除。

旧单歌单 `managed_songs`、`sync_runs` 和绑定表在迁移 `0008_multi_playlist_monitoring.sql` 期间保留，避免迁移先于 Worker 发布时使旧版本立即失效。新接口只使用多歌单表。同步批次和逐歌单任务分别存在 `sync_batches`、`sync_playlist_tasks`，已结束的批次保留最近 30 天；调整监控集合时，旧集合的批次结果会清理，避免呈现为新集合的结果。

## 同步流程

Cron 保持 `17 19 * * *`，即北京时间每日 03:17。它与“同步状态”页主按钮都创建全量批次。`MusicBatchWorkflow` 通过 D1 状态锁按网易列表顺序派发每歌单一个 `MusicSyncWorkflow`，等待该任务结束后才派发下一个；每日批次遇到正在执行的手动任务会排队。每个歌单页的按钮只创建该歌单的手动批次。批次或临时基线在运行时，新的手动按钮禁用。

歌单级读取或复核失败会在 Workflow 重试结束后记为失败，并继续后续歌单；登录失效、UID 不符或风控是账号级错误，后续歌单保持未执行。每个状态提交以歌单版本、账号 UID、正在运行的任务为条件，歌曲资料、逐歌单状态和成功结果同处一次 D1 `batch()`。不完整快照或复核中变化的成员集合不会写入歌曲状态。

首次绑定仍要求非空歌单。已建立基线的歌单若后来清空，在两次完整读取且成员都稳定为空后，可将该歌单原有正常歌曲确认为消失；零成员本身不能使基线标志丢失。

## 页面与接口

左侧顺序是“待找回 → 同步状态 → 歌单歌曲”；已监控歌单展开在“歌单歌曲”下，移动端使用选择抽屉。页面打开期间保留当前歌单；重新打开时先选特殊类型 `5` 的“我喜欢的音乐”，否则选四项指标总数最大的歌单，平局按网易列表顺序。切换会作废上一歌单的分页和搜索请求。

歌单页上方显示该歌单专属的进度卡、五个步骤和歌曲总数／正常／变灰／消失四项数字，下面才是歌曲列表。独立“同步状态”页展示批次进度、当前歌单及逐歌单结果，不跨歌单相加歌曲数。全量批次结束后，打开中的网站显示成功、失败、未执行汇总，结果也留在同步状态页。

待找回按歌曲 ID 去重。任一歌单消失则状态列显示“消失”，否则有变灰则显示“变灰”；歌单标签始终显示各自真实状态，先显示两个，余项可展开。状态筛选匹配底层歌单状态，因此混合状态歌曲同时出现在两个筛选中；歌单筛选只约束入选歌曲，仍显示该歌曲的全部异常标签。“最后正常”取全部异常上下文中最早的非空时间。点击“完成”会删除该歌曲在全部歌单中的异常状态，保留其他正常引用；同步或调整歌单期间暂不可完成，以免正在提交的快照重新写入异常。待找回行不提供失效的网易跳转，歌单歌曲页跳转仍保留。

接口约定：`POST /api/playlist-binding` 接收 `playlistIds` 数组；取消监控时还需 `confirmedRemoval: true`。重建全部已选歌单基线时使用 `freshBaseline: true` 与 `confirmedFreshBaseline: true`，只允许当前账号执行。`POST /api/sync` 可选 `playlistId`，省略则同步全部。`GET /api/likes` 使用 `playlistId` 查询当前歌单；`GET /api/recovery` 可用 `type`、`playlistId`、`query` 筛选；`GET /api/sync/status` 返回监控歌单、最新批次、最近完成批次和每歌单任务。所有修改请求继续受 Cloudflare Access、同源检查和共享网易会话的账号校验保护。

## 运维

线上升级先导出远程 D1，备份放在 Windows 桌面，再运行项目正式迁移与完整发布流程。发布物仍是 Worker、Static Assets、两个 Workflow 和原 Cron；无需服务器端另装 npm 依赖。在线检查时，分别查看单歌单手动同步、全量同步、逐歌单任务结果和待找回聚合。`package.json` 与锁文件不需要改变。
