# 未来开发方向备忘录

这个文件用于长期记录已经想到、但当前阶段暂不实施或暂时无法解决的开发方向。这里的条目不是已经承诺的版本计划，也不代表已经完成技术验证；开始实现前，仍需重新梳理需求、安全边界、依赖变化、兼容性、测试和发布流程。

记录新条目时不得写入真实 Cookie、Secret、API Token、邮箱验证码或其他凭据，也应尽量避免记录不必要的个人信息和真实基础设施标识。

## 1. 使用 Agent 辅助小白完成首次部署（`v0.1.0` 已完成首版）

### 想解决的问题

这个项目的目标用户可能是纯代码小白。即使提供了完整源码和普通部署文档，用户仍可能不知道怎样检查开发环境、登录 Cloudflare、创建并绑定 D1、应用迁移、生成并保存加密密钥、部署 Worker、配置 Cloudflare Access、扫码连接网易云以及验证首次同步。

`v0.1.0` 已把项目设计成对通用 Agent 友好的“可执行安装包”：用户取得项目后，在项目根目录向能够运行 Windows 终端的 Agent 提出一段自然语言指令，由 Agent 调用正式脚本完成确定性的安装、配置、部署和验收工作。

### 期望体验

Agent 可以负责：

1. 检查操作系统、Node.js、npm、Wrangler 和项目依赖；
2. 引导用户登录并授权自己的 Cloudflare 账户；
3. 创建或复用这个用户专属的 D1、Worker、Workflow、Cron 和其他必要资源；
4. 应用数据库迁移；
5. 生成 `SESSION_ENCRYPTION_KEY`，通过 Cloudflare Secret 安全保存；
6. 收集并写入该实例所需的非敏感配置；
7. 构建、部署并完成基础验收；
8. 协助配置 Cloudflare Access；如果 Agent 无法直接操作浏览器，则给出精确、短小的人工步骤；
9. 引导用户使用网易云 App 扫码，并验证首次完整同步。

理想情况下，用户只需要亲自完成无法或不应代办的动作，例如提供允许访问的邮箱、登录并授权 Cloudflare、接收 Access 验证码、用网易云 App 扫码确认，以及确认会改变其云端账户的操作。

### 不应只依赖一段提示词

根目录可以提供一个显眼的 Agent 入口提示，但可靠部署不能只依赖自由发挥的长提示词。未来应考虑同时提供：

- 面向 Agent 的仓库级操作说明；
- 面向普通用户和不同 Agent 的通用安装说明；
- 可重复、可校验、失败后可安全重试的部署脚本；
- 必要时提供项目专用的 Agent Skill；
- Agent 能控制浏览器时的自动路径，以及不能控制浏览器时的人工回退路径。

### 安全与行为约束

- Agent 不得把真实凭据写进 Git、受跟踪配置、终端日志或聊天输出；
- 不得关闭审批、绕过账户授权或静默执行高影响外部操作；
- 创建、修改或删除 Cloudflare 资源前，应说明目标和影响；
- 重复执行时应优先识别并复用本用户已有资源，避免制造重复实例；
- 部署结束后必须验证 Access、D1 migration、Secret、Workflow、Cron 和网易账号绑定是否正确；
- 不应为了适配某台机器而临时修改业务逻辑；
- 自动化流程应基于明确的开源发行版本，而不是不可复现的本地工作区状态。

### 可能形成的三条使用路径

项目目前同时提供：

1. Deploy to Cloudflare，作为无需下载的首选网页入口；
2. Agent 辅助部署，作为 Windows 本地自动化路径；
3. 完整手动文档，作为透明、可审计的兜底路径。

### 当前状态

首版已实现：根目录 Agent 提示词、Windows 幂等初始化脚本、共享部署程序、官方部署按钮和完整手动文档。仍可在未来扩展 macOS/Linux 本地脚本、项目专用 Skill，以及能安全控制 Cloudflare 控制台的浏览器自动化；这些扩展不得改变现有凭据边界。

## 2. 开源项目更新后的低门槛升级与纯在线部署

### 忠实记录的问题

项目正常开源并产生后续更新以后，纯代码小白怎样更方便地把新版本部署到自己独有的 Cloudflare 实例？是否可以让用户不下载项目，就直接在线完成首次部署或后续升级？

需要同时保留“一名用户一套独立实例、一个实例只管理一个网易账号和一个歌单”的前提，并考虑怎样避免升级覆盖个人配置、Secret、D1 数据和 Cloudflare Access 设置。

### 当前状态

本条只记录问题。本阶段不开展方案设计、技术选型或可行性结论；等项目达到正常开源和稳定发行阶段后再讨论。

## 3. 一个网易账号同时绑定并监控多个歌单

### 想解决的问题

当前版本坚持一个实例只绑定一个网易账号和一个歌单。未来可以评估让同一账号在同一个实例内选择并同时监控多个可访问歌单，包括自建、私密和收藏的他人歌单。

### 必须先完成的模型改造

这不是简单增加一个选择框。届时需要为歌单绑定、歌曲状态、异常确认、同步任务、手动同步队列、同步历史和并发版本控制统一增加 `playlist` 维度，并明确同一首歌出现在多个歌单时是共享歌曲资料、分别保存成员状态和异常历史。定时任务还需要处理分页、并发上限、失败隔离和单个歌单重绑，避免一个歌单的读取失败影响其他歌单。

### 当前状态

仅记录方向。本次网易登录与歌单绑定改造不实施多歌单数据模型，也不为尚未确定的需求预留半成品字段。

## 本次需求的依赖变更

- 依赖变更：无。
- 未新增、升级、替换或删除 npm 依赖。
- `package.json` 的依赖区与 `package-lock.json` 的依赖解析不变；`package.json` 仅调整项目脚本以复用正式迁移、Secret 检查和部署程序。
- 现有依赖与 Cloudflare Worker + Static Assets + Workflow 的完整发布形式保持兼容。
- 发布时仍构建并部署完整 Worker 与静态资源；不需要服务端另行安装依赖，也不存在薄包/全包切换。

## 后续条目模板

### 方向名称

- 想解决的问题：
- 使用场景：
- 必须保持的约束：
- 暂不解决的范围：
- 何时重新评估：
- 当前状态：仅记录 / 待讨论 / 已立项 / 已放弃
